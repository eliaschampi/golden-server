package logger

/* file, err := os.OpenFile("./mylog.log", os.O_RDWR|os.O_CREATE|os.O_APPEND, 0666)
if err != nil {
	log.Fatalf("error opening file: %v", err)
}
defer file.Close()


type

app.Use(logger.New(logger.Config{
	Output: file,
}))
*/
